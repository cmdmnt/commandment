import {Map} from "immutable";
import {IToggleRowSelectionAction} from "./actions";

export const TOGGLE_ROW_SELECTION = (state: Map<string, any>, action: IToggleRowSelectionAction) => {
    console.log("bla");
    return state;
};
