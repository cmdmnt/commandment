version: "3"
services:
  commandment:
    build:
      context: .
      dockerfile: .docker/Dockerfile
    image: commandment:latest
    volumes:
      - "./.docker/settings.cfg.docker:/settings.cfg"
    ports:
      - "8120:80"