version: "3"
services:
  commandment:
    build:
      context: .
      dockerfile: .docker/Dockerfile
    image: commandment:latest
    volumes:
      - "./.docker/settings.docker.cfg:/settings.cfg"
    ports:
      - "8120:80"